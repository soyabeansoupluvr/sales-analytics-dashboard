# Development and CI dependencies.
#
# Installed on top of requirements.txt in the CI workflow and in
# developer environments. Never installed on a production runtime.

-r requirements.txt

# Testing and coverage
pytest==8.2.2
pytest-cov==5.0.0

# Linting and formatting
flake8==7.1.0
black==24.4.2
