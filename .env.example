# --- Streamlit ---------------------------------------------------------------
STREAMLIT_SERVER_PORT=8501
STREAMLIT_SERVER_ADDRESS=localhost

# --- Storage (M7 Protected Store) --------------------------------------------
DATA_RAW_DIR=data/raw
DATA_PROCESSED_DIR=data/processed
SQLITE_URL=sqlite:///data/processed/dashboard.db

# --- Security & Ethics (M6 + M8) ---------------------------------------------
# HMAC key used to pseudonymize CustomerID at ingestion. Rotate quarterly.
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
PSEUDONYM_KEY=change-me-generate-32-bytes-hex

# Minimum cohort size before segments are shown to non-admin users.
SMALL_GROUP_SUPPRESSION_THRESHOLD=5

# Session throttle (requests per minute per authenticated user).
RATE_LIMIT_RPM=120

# --- Fairness sensitivity for RFM segmentation (M4) --------------------------
# Holdout fraction used for the adjusted Rand index stability check. 0.1 means
# 90% of customers train the reference model and the last 10% are the holdout.
RFM_HOLDOUT_FRACTION=0.1

# Minimum ARI score at which the segmentation is considered stable enough to
# report without a caveat. Below the threshold, the M5 caption surfaces the
# lower score so consumers can weigh the finding accordingly.
RFM_STABILITY_THRESHOLD=0.7

# --- Logging (M14) -----------------------------------------------------------
LOG_LEVEL=INFO
LOG_FORMAT=json
LOG_DIR=logs
